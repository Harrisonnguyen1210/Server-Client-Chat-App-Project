<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ToolBar_Theme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/toolBarText</item>
        <item name="android:actionMenuTextColor">@color/actionMenuColor</item>
        <item name="colorControlNormal">@color/actionMenuColor</item>
        <item name="android:colorControlActivated">@color/controlActivated</item>
    </style>

    <style name="ToolBar_Title">
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/toolBarText</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textSize">20sp</item>
    </style>
</resources>
